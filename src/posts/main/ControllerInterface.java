package posts.main;

/**
 * Interface für alle Controller in der Anwendung.
 * Jeder Controller muss die `init()`-Methode implementieren.
 */
public interface ControllerInterface {
    /**
     * Initialisiert den Controller.
     */
    void init();
}